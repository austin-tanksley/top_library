const MY_LIBRARY = [];
const libList = document.querySelector("#libList");

function book(title, author) {
    this.title = title;
    this.author = author;
}

function displayLib(){
    for (book in MY_LIBRARY){
        
    }
}